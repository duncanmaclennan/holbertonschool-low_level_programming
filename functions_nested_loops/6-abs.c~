#include "main.h"
/**
 * _islower - This is a description
 * @c:  this is the character passed
 * Return: returns 1 if true, 0 if false
 */
int _abs(int)
{
	if (int > 0)
	{
		return -int;
	}
	else
	{
		return int;
	}
}
