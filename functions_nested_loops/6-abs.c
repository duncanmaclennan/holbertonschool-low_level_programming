#include "main.h"
/**
 * _abs - This is a description
 * @n:  this is the character passed
 * Return: returns 1 if true, 0 if false
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
