#include "main.h"
/**
 * _islower - This is a description
 * int c - this is the character passed
 * Return - This is the return action
 */
int _islower(int c)
{
if (c > 96)
{
	return (1);
}
else
{
	return (0);
}
}
