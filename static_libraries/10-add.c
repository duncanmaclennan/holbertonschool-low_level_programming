#include "main.h"
/**
 * add - This is a description
 * @m: the first number to add
 * @n: the second number to add
 * Return: returns the result
 */

int add(int m, int n)
{
	int x;

	x = m + n;
	return (x);
}
