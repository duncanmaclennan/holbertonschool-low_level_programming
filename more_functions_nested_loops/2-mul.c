#include "main.h"
/**
 * mul - tests if character is digit
 * @a: the first integer
 * @b: the second integer
 * Return: 1 if true, 0 if false
 */

int mul(int a, int b)
{
	int c;

	c = a * b;

	return (c);
}
