#include "main.h"
/**
 * print_most_numbers - tests if character is digit
 * Return: the output
 */
void print_most_numbers(void)
{
	int n;

	n = 0;
	while (n < 10)
	{
		if (n != 2 && n != 4)
		{
			_putchar('0' + n);
		}
		n = n + 1;
	}
}
