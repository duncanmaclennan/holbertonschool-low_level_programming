#include <stdio.h>
/**
 * _atoi - tests if character is digit
 * @s: the first integer
 * Return: the conversion
 */
void reverse_array(int *a, int n) {
    int i, temp;

    for (i = 0; i < n / 2; i++) {
        temp = a[i];
        a[i] = a[n - i - 1];
        a[n - i - 1] = temp;
    }
}
