#include <stdio.h>
/**
 * _atoi - tests if character is digit
 * @s: the first integer
 * Return: the conversion
 */
char *_memset(char *s, char b, unsigned int n) {
    unsigned int i;
    for (i = 0; i < n; i++) {
        s[i] = b;
    }
    return s;
}
